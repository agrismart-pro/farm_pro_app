name: agrismart_pro
description: "Application AgriSmart Pro - Gestion de Ferme"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # المكتبات الضرورية اللي استعملنا في الكود
  supabase_flutter: ^2.0.2
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
